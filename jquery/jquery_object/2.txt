<h1>要素の集合と繰り返し処理</h1>

$()で要素を指定してメソッドを実行すると、指定した要素すべてに対して同じメソッドが一度に実行される。
指定した要素が1つの集合として管理され、その集合に収められたそれぞれの要素に対して繰り返し同じメソッドを
実行するようになっているからです。

<h2>要素やその集合を取得する</h2>

・Arrayに似た働きをする
・要素の集合全体を配列で取得する
・特定の位置の要素のみを抽出する


集合に対して繰り返し処理する

・eachメソッド

・戻り値　→　関数の処理に用いた集合
・関数の
